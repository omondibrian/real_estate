import React from "react";
interface TenantProps {
  /**
   * Optional click handler
   */
  onClick?: () => void;
}
/**
 * display sidebar navigaion items
 */
export const TenantInfo = (props:TenantProps) => {
  return <div>TenantInfo</div>;
};
